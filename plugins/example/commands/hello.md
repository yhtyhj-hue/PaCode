---
description: Greet the user via the example plugin
---

Greet the user warmly and mention that this response was triggered by the **example** plugin command `/hello`.

If the user provided arguments via `$ARGUMENTS`, incorporate them into the greeting.
